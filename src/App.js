import React from "react";
import Average from "./6_10/Average";

const App = () => {
  /*const [visible, setVisible] = useState(false);

  return (
    <div>
      <button
        onClick={() => {
          setVisible(!visible);
        }}
      >
        {visible ? "숨기기" : "보이기"}
      </button>
      <hr />
      {visible && <Info />}
    </div>
  );
  */
  return <Average />;
};

export default App;
